[tool.poetry]
name = "bitrise_reports"
version = "0.0.1"
description = ""
authors = ["Your name <your@email.com>"]
license = "MIT"
readme = "README.md"
homepage = "https://github.com/<repo-owner>/bitrise_reports"
repository = "https://github.com/<repo-owner>/bitrise_reports"
include = [ "LICENSE.md" ]
classifiers = [
    "Environment :: Console",
    "Operating System :: OS Independent"
]

[tool.poetry.dependencies]
python = "^3.8"
flake8 = "3.9.0"
requests = "^2.25.1"
responses = "^0.13.0"
black = "^20.8b1"
bandit = "^1.7.0"

[tool.poetry.dev-dependencies]
pytest = "6.2.2"
pytest-cov = "2.11.1"

[tool.poetry.scripts]
bitrise-reports = "bitrise_reports:main"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
